server:
  port: 8081
  servlet:
    context-path: /gpio/

spring:
  application:
    name: "@project.artifactId@"
  build:
    version: "@project.version@"
    timestamp: "@maven.build.timestamp@"
  main:
    banner-mode: off
